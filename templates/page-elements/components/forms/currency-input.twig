<input type="text"
    class="form-control disabled form-input-currency"

    {% for finance_currency_dto in getFinancesCurrenciesDtos() %}

        {% if
                currency is defined
            and currency == finance_currency_dto.getName()
        %}

        value="{{ finance_currency_dto.getSymbol() }}"
        {% endif %}
    {% endfor %}
/>