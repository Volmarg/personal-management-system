{% if ajax_render == false %}
    {% set template= "base.html.twig" %}
{% else %}
    {% set template= "blank.html.twig" %}
{% endif %}

    {% extends template %}

{% block body %}
    <!-- ### $App Screen Content ### -->
    <main class='main-content bgc-grey-100'>
        <div id='mainContent'>
            <div class="container-fluid">

                <div class="row">
                    <div class="col-md-12">
                        <div class="bgc-white bd bdrs-3 p-10 mB-20">
                            <h4 class="c-grey-900 mB-20 text-center">{{ 'notes.settings.headers.settings.label' | trans }}</h4>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-12">
                        <div class="bgc-white bd bdrs-3 p-10 mB-20">
                            <h4 class="c-grey-900 mB-20 text-center">{{ 'notes.settings.headers.categories.label' | trans }}</h4>
                            <h3>{{ 'notes.settings.headers.categories.editCategory' | trans }}</h3>
                            {% include 'modules/my-notes/components/categories-settings.table.html.twig' %}

                            <h3>{{ 'notes.settings.headers.categories.addCategory' | trans }}</h3>
                            <div class="add-record-form">
                                {{ form_start(form,{'attr':{' data-entity':"MyNotesCategories"}}) }}
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </main>

{% endblock body %}
