{% if ajax_render == false %}
    {% set template= "base.html.twig" %}
{% else %}
    {% set template= "blank.html.twig" %}
{% endif %}

    {% extends template %}

{% block body %}
    <!-- ### $App Screen Content ### -->
    <main class='main-content bgc-grey-100' id="MyPaymentsOwedModuleBody">
        <div id='mainContent'>
            <div class="container-fluid">
                {# Owed by others #}
                <div class="row">
                    <div class="col-md-12">
                        <div class="bgc-white bd bdrs-3 p-20 mB-20">
                            <h4 class="c-grey-900 mB-20 text-center">{{ translate('payments.moneyOwed.headers.owedByOthers') }}</h4>
                            {% include 'modules/my-payments/components/owed.table.html.twig' with {
                                'records'   : owed_by_others,
                                'summarries': summary_owed_by_others
                            } %}
                        </div>
                    </div>
                </div>
                {# Owed by me #}
                <div class="row">
                    <div class="col-md-12">
                        <div class="bgc-white bd bdrs-3 p-20 mB-20">
                            <h4 class="c-grey-900 mB-20 text-center">{{ translate('payments.moneyOwed.headers.owedByMe') }}</h4>
                            {% include 'modules/my-payments/components/owed.table.html.twig' with {
                                'records'   : owed_by_me,
                                'summarries': summary_owed_by_me
                            } %}
                        </div>
                    </div>
                </div>
                {# Totall summary#}
                <div class="row">
                    <div class="col-md-12">
                        <div class="bgc-white bd bdrs-3 p-20 mB-20">
                            <h4 class="c-grey-900 mB-20 text-center">{{ translate('other.overallSummary') }}</h4>

                            <h5 class="text-center"><u>{{ translate('payments.moneyOwed.headers.owedByMe') }}</u></h5>
                            {% include 'modules/my-payments/components/owed-overal-summary.html.twig' with {"summarries": summary_overall_owed_by_me} %}

                            <h5 class="text-center"><u>{{ translate('payments.moneyOwed.headers.owedByOthers') }}</u></h5>
                            {% include 'modules/my-payments/components/owed-overal-summary.html.twig' with {"summarries": summary_overall_owed_by_others} %}
                        </div>
                    </div>
                </div>
                {# Form #}
                <div class="row">
                    <div class="col-md-12">
                        <div class="bgc-white bd bdrs-3 p-20 mB-20 d-flex flex-column">
                            <h4 class="c-grey-900 mB-20 align-self-center">{{ translate('payments.moneyOwed.headers.addRecord') }}</h4>
                            <div class="col-md-6 align-self-center add-record-form">
                                {{ form_start(form,{'attr':{' data-entity':"MyPaymentsOwed"}}) }}
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

{% endblock body %}
