{# Todo: add some predefined images for this module - platforms, ignore upload #}
{# Todo: clean the template #}

<div class="contact-area ">
    <div class="contact contact-card-border contact-card-no-padding" style="width: 100%;">
        <main>
            <section>
                <div class="content" style="padding-bottom: 20px; background-color: {{ contact.getNameBackgroundColor() }};">
                    <img src="{{ contact.getImagePath() }}" alt="Profile Image" style="width: 60px; height: 60px;">
                    <aside style="padding-top:10px;">
                        <h1 style="font-size:18px;">{{ contact.getName() }}</h1>
                    </aside>
                </div>

                <div class="title active" style="background-color: {{ contact.getDescriptionBackgroundColor() }};">
                    <p style="text-transform: none; font-size: 12px;">{{ contact.getDescription() }}</p>
               </div>
            </section>

        </main>

        <nav class="active contact-cards-contacts-wrapper">

            {% for contact_type in contact.getContacts().getContactTypeDtos() %}

                <a href="#" >
                    <div class="icon" style="width: 35px; height: 35px;">
                        <img src="{{ contact_type.getIconPath()  }}" style="width:100%;"/>
                    </div>

                    <div class="content">
                        <h1>{{ contact_type.getName() | capitalize }}</h1>
                        <span>{{ contact_type.getDetails() }}</span>
                    </div>
                </a>

            {% endfor %}

        </nav>
    </div>
</div>