<table id="MyContacts" class="table table-striped table-bordered" cellspacing="0" data-table="true" width="100%">
    <thead>
    <tr>
        <td class="d-none">{{ translate('datatables.table.id') }}</td>
        <td>{{ translate('contact.tables.contact') }}</td>
        <td>{{ translate('contact.tables.description') }}</td>
        <td class="d-none">{{ translate('contact.tables.type') }}</td>
        <td class="">{{ translate('contact.tables.group') }}</td>
        <td>{{ translate('datatables.table.actions') }}</td>
    </tr>
    </thead>
    <tbody>
    {% for contact in contacts %}
        <tr class="trash-parent car-single-data editable-parent save-parent"
            data-type="MyContacts{{ type | capitalize }}">
            <td class="d-none id">{{ contact.id }}</td>
            <td class="contact"> {{ contact.contact }}</td>
            <td class="description"> {{ contact.description }}</td>
            <td class="type d-none"> {{ type }}</td>

            <td class="parent-td">

                <select class="form-control disabled group option-picker">
                    <option value=""></option>
                    {% for group in groups %}
                        <option value="{{ group.id }}"
                                {% if contact.group is not null and group.id  == contact.group.id %}selected{% endif %}>
                            {{ group.name | capitalize }}
                        </option>
                    {% endfor %}
                </select>
            </td>


            {% include 'modules/common/components/table-cells/actions.html.twig' with {
                "trash": true,
                "edit": true,
                "save": true
            } %}

        </tr>
    {% endfor %}
    </tbody>
</table>
