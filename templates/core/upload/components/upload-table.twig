<table id="uploadTable" class="table table-striped table-bordered" cellspacing="0" width="100%"> {# this table is initialized in upload.js #}
    <thead>
        <tr>
            <th style="max-width: 100px;">{{ 'datatables.table.idHash' | trans }}</th>
            <th style="max-width: 300px;">{{ 'upload.uploadTable.fileName' | trans }}</th>
            <th style="max-width: 150px;">{{ 'upload.uploadTable.fileExtension' | trans }}</th>
            <th>{{ 'upload.uploadTable.tags' | trans }}</th>
        </tr>
    </thead>

    <tbody>
        {# Filled via js #}
    </tbody>
</table>