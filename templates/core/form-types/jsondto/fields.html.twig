{# This form allows to add multiple fields (on front via js) that will be later transformed into json #}
{% block jsondto_widget %}
        <input {{ block('widget_attributes') }} autocomplete="off" class="form-control col-sm-6" data-json-dto-main-input="true">
{% endblock %}